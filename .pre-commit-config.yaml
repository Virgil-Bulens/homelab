repos:
  - repo: https://github.com/adrienverge/yamllint
    rev: v1.35.1
    hooks:
      - id: yamllint
        args: [--strict, --config-file=.yamllint]
        # Helm templates with Go template directives are not valid standalone YAML
        exclude: ^infrastructure/authentik/templates/blueprint-example\.yaml$
