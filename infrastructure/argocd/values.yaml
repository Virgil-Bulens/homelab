---
argo-cd:
  configs:
    params:
      # TLS terminated at the Gateway using the wildcard cert.
      # argocd-server serves plain HTTP; Gateway proxies HTTPS → HTTP.
      server.insecure: "true"
