# PLACEHOLDER — generate and replace before pushing.
# WEBUI_SECRET_KEY is used for signing session cookies.
#
# 1. Generate a random secret:
#    SECRET=$(openssl rand -hex 32)
#
# 2. Seal it:
#    kubectl create secret generic open-webui-secret -n ai \
#      --from-literal=WEBUI_SECRET_KEY="$SECRET" \
#      --dry-run=client -o yaml | \
#      kubeseal --controller-name=sealed-secrets \
#               --controller-namespace=sealed-secrets \
#               -o yaml > infrastructure/ai/templates/open-webui-secret-sealed.yaml
