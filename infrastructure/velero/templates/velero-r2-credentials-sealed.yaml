# Seal with:
#   kubectl create secret generic velero-r2-credentials \
#     --namespace velero \
#     --from-literal=cloud="[default]\naws_access_key_id=<R2_TOKEN_ID>\naws_secret_access_key=<R2_TOKEN_SECRET>" \
#     --dry-run=client -o yaml \
#   | kubeseal --controller-name=sealed-secrets --controller-namespace=sealed-secrets \
#     --format yaml > infrastructure/velero/templates/velero-r2-credentials-sealed.yaml
#
# R2 token values from: terraform -chdir=infrastructure/cloudflare-r2 output -raw velero_r2_api_token
# R2 access key ID = Cloudflare API token ID (visible in Cloudflare dashboard after apply)
#
# PLACEHOLDER — replace this file with the sealed output before committing.
